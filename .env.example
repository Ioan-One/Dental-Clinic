DATABASE_URL="postgresql://user:password@localhost:5432/dental_clinic?schema=public"
JWT_SECRET="your-strong-secret-here"

# App URLs (used in email links)
APP_URL="https://yourdomain.com"
FRONTEND_URL="http://localhost:5173"

# Optional: SMTP config for production email sending
# If not set, Nodemailer uses Ethereal (fake SMTP) — preview links appear in server console
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""
